<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Autenticación google</title>
    <style>
        body{
            font-family: Arial, "Helvetica Neue", Helvetica, sans-serif;
            margin: 0
        }
        nav {
            background: #ff5353;
            padding: 20px 10px;
        }
        nav ul {
            max-width: 960px;
            margin: 0 auto;
            padding: 0
        }
        nav li {
            list-style-type: none;
            display: inline-block;
            margin: 0 10px 0 0;
        }
        nav a {
            color: #fff;
            font-size: 18;
            background: rgb(255,255,255,0.2);
            text-decoration: none;
            padding: 10px;
            display: block;
        }
        main,header {
            max-width: 960px;
            margin: 30px auto;
            padding: 0 10px;
            color: #333;
        }
        .parrafo {
            margin-left: 70px;
        }
        .fotoperfil {
            width: 10%;
            height: 10%;
            margin-left: 70px;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <nav>
        <ul>
            <% if (user) {%>
            <li><a href="/logout">Logout</a></li>
            <li><a href="/profile">Profile</a></li>
            <%} else {%> 
            <li><a href="/login">Login</a></li>
            <%} %>
            <li><a href="/">Homepage</a></li>
        </ul>
    </nav>
    <img class="fotoperfil" src="<%= user.imagen %>" alt="Foto de perfil">
    <p class="parrafo">Bienvenido, tu usuario es <%= user.username %> <br><br> Su numero de telefono es</p>
    
    
    
</body>
</html>